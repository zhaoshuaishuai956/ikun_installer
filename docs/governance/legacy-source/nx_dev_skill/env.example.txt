# scripts/add-pitfall.ps1 提交时可选的环境变量覆盖。
# 全部可不设——脚本均有默认值。把本文件复制为 .env 并按需填写（.env 已被 .gitignore 忽略，不会提交）。

# 提交署名覆盖（默认 "Claude Code <noreply@anthropic.com>"）
# GITEA_COAUTHOR="Your Name <you@example.com>"

# 设备名覆盖（默认取 $env:COMPUTERNAME；取不到则 unknown-device）
# GITEA_DEVICE="my-machine"
