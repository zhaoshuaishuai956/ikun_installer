# 红队批 1 审查报告（执行期：CI 闸门 + 菜单归一 diff）

> 审查对象：`ikun_installer` 已推送 diff（M3/M7a/M7b/M4/M5/M8b，c33e0e7..e1b23db）。
> 时间：2026-08-16。只读，未改任何文件。审查期间 HEAD 推进到 80a20a4（M6/M9/M10）与 b25973c（M9/M14）。

## P0（1 项，已随并发提交修复）
1. **G3 用 objdump -p 判 PE 导出会在目标 CI 误报 → 打包失败**：rock5t（linux arm64, binutils 2.40）上 objdump -p 输出格式与判据不匹配，对全部 17 个 dll 报「缺少 ufusr」→ pack 被阻断。已改 `strings -a`（PE 导出名表为 ASCII 明文，本地实证全含双导出）。

## P1（6 项，全部闭环）
2. **G5c/G5d 大小写 bug → MultiEntityNester 的 BITMAP/LABEL 校验被静默跳过**：act2btn 键小写化但取用按原大小写。修复：`act2btn.get(repo.lower() + ".dll", [])`。这是 G7 文本比对兜底链的断点（UTF-8 误存菜单的唯一防线），M1 改名窗口期尤其关键。
3. **G6 secret 扫描在 depth-1 克隆下空转**：origin/master==HEAD → diff 恒空。修复：pack.yml `git fetch --unshallow` + 基取 `HEAD~1`。
4. **G6 禁止入库物清单漏 `.user` 与目录/凭证类**：正则补 `user`，并加 `bin/ obj/ publish/ ref/ refint/`、`.env`、`*.token`、`secrets/` 检查。
5. **Form1.cs 完整性校验仍引用已删除的旧 bmp 名**（ikun_ejector.bmp/ikun_pmi.bmp）→ 每次安装虚假警告。改为新名。
6. **G8 只警告不阻断 + 漏 ikun_updater.dll**：阶段一保持警告（渐进），补 ikun_updater.dll 纳入（gates.sh 按安装器仓 HEAD 比对）。
7. **icon_cn 白名单与规范不一致（放行 A-Z）**：选择规范勘误方向（v1.0.1：1–2 汉字或 1–3 大写字母），gate 与规范同步。

## P2（已处置/登记）
8. G4 制品检测硬编码扩展名 → 改为从 ikun-deploy.txt globs 派生（含 .def）。
9. G7「BUTTON…END_OF_BUTTON」与 NX 菜单方言不符 → 规范勘误（v1.0.1）。
10. G5 补 LABEL 互不重复 + 孤儿 bmp 警告。
11. G5 双向校验由 verify-repo.ps1 带外承担（分工成立，登记）。
12. M4 meta 摘要 3 列 vs 实现 5 列 → 规范勘误字段序（v1.0.1）。
13. M5 空 application/ 无 .gitkeep → 已强制入库 .gitkeep。
14. dev-assemble.ps1 sslVerify=false → 移除（§11.6 证书策略），提示依赖系统信任链。
15. G4 开 Issue 与 M11 双 token 对齐：write:repository 覆盖 issues，M11 落地后不受影响。
16. G4 首跑基线噪声：M2 后并入仓全部有 CHANGELOG，不再触发（实证 run 84 无 G4 警告）。

## 菜单一致性核对（c 角度）
- LABEL vs name_cn：7/7 逐字一致；BITMAP 归一正确；GBK 解码干净。
- M4 handoff 链路（assemble→tsv→gen-icons→gates→check_consistency）成立，字段序三端一致。

## 总评
批 1 方向正确、菜单归一落地扎实，但有 1 个会导致 CI 失败的 P0（已修复）和 6 个判据漏洞——最要命的是 G5 大小写 bug 与 G6 secret 扫描空转，均已闭环。
