# 红队批 2 审查报告（执行期：更新链 C# 改造 M6/M9/M10）

> 审查对象：`ikun_installer` 已推送 diff（80a20a4 M6/M9/M10 + ae9fc13 红队批1 闭环）。只读。
> 时间：2026-08-16。结论：**当前不可合并/发布，须先修 P0-1 与 P1-1/2/3** —— 已全部闭环（提交 77aeb9b）。

## P0（1 项，已修）
1. **VerifyDownloadedInstaller「无签名 + sha256 匹配」返回 false → 更新链自我中断**
   - 证据：`UpdateManager.cs:215` `return sig != false;` 与注释「无签名但有 sha256 也放行」相反。
     过渡期所有构建 sig==false，而 publish-release.sh 已在正文写 sha256 → 新构建装完后对下一个
     （同样无签名、带 sha256）release 一律拒更。
   - 修复：过渡期恒放行（`return true`），M13 落地时**同一版本原子**改为 `return sig == true`（注释已标注切换点）。

## P1（3 项，已修）
2. **install_dir 写回未实现**：AppConfig/ikun_updater 声称安装器写回 install_dir，实际无代码写。
   修复：Form1 安装成功即写注册表 `install_dir` = 实际安装目录。
3. **install_dir 无校验 → 提权流程写任意目录**：修复：`AppConfig.IsSafeInstallDir`（绝对路径/无 `..`/无引号）接入安装入口。
4. **下载落 %TEMP% 共享目录 + TOCTOU 未收敛**：修复：下载目录改 `%LOCALAPPDATA%\ikun_tools\downloads`（规范 §9.3.3）。

## P2（7 项，已处置）
5. 三段/四段 Version 比较差异：真实链路恒四段，登记不修。
6. `int.Parse` 溢出：改 `int.TryParse`。
7. 预发布后缀未拒绝：正则加 `(?=\s*(?:\(|$))` 后缀锚点，拒绝 `-beta`/尾点。
8. `sha256sum || true` 会静默产出空哈希：改缺失即失败。
9. 测试缺口：Versioning 补 6 用例（溢出/后缀/尾点/尾空格/Tab 后缀）；VerifyDownloadedInstaller 的签名策略
   为 Windows 专属（Authenticode via PowerShell），在移植测试外，其决策逻辑已由 P0 修复评审覆盖。
10. `.gitkeep` 被 EmbeddedResource 收录：csproj Exclude。
11. ikun_updater.cpp install_dir 无白名单：加白名单（字母/数字/空格/`:\._-`），非法回退默认值。

## 回归确认
grep `UpdateManager.GetLocalVersion|ParseRemoteVersion|IsNewer|DefaultProxy|GiteaLatestApi` 无残留；
Form1/UpdateCheckForm 全部改调 Versioning.*/AppConfig.*；csproj 仍嵌入资源（排除 .gitkeep）。

## 附加发现（G3 两次 CI 实证）
- objdump -p（binutils 2.40 arm64）与 strings|grep 管线在容器均误报 → G3/G8 改 python null 终止
  字节判定（本地 17 个 dll 全验：`b"ufusr\x00"` + `b"ufusr_ask_unload\x00"` 全部命中）。

## 总评
版本解析与 JSON 强类型化方向正确，但 P0 的「注释与实现相反」会在下一版发布后直接打断更新链；
P1 的 install_dir 写回缺失与可配置目录无校验属真实风险。全部闭环后经 run 90 实证。
