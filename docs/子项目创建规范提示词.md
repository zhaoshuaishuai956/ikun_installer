# 子项目创建规范提示词（兼容入口）

权威的完整 AI 提示词已合并到 `docs/AI-NX二次开发提示词.md`。新任务请复制该文件的“主提示词”；
规则以 `docs/NX二次开发权威规范.md` 为准。

最低不可省略要求：先询问是否并入爱坤；中文功能名正好四个汉字；并入插件在 `ufusr` 中接入
`CheckOnceOnFirstPluginUse()`；`ufusr_ask_unload` 返回 `UF_UNLOAD_IMMEDIATELY`；已有插件支持原位热更新，
仅新增插件需要重启 NX。
